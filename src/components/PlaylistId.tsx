/* import { getUserPlaylistTracks } from "../API/fetchServices/fetchPlaylistTracks";
import { usePlaylistDataState } from "./states/playlistDataState";

const { playlistIds } = usePlaylistDataState();

// Assuming you want to get tracks for the first playlist in the array
getUserPlaylistTracks(playlistIds)
  .then((tracks) => {
    console.log(tracks);
  })
  .catch((error) => {
    console.log("this is error:", error);
  });
 */
